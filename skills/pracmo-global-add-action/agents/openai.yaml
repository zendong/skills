interface:
  display_name: "Pracmo Add Action"
  short_description: "Add grounded Actions to an existing Pracmo Track"
  default_prompt: "Use $pracmo-global-add-action to find one of my existing Pracmo Tracks and add an Action. For follow-along images, retrieve reliable sources, verify posture, sequence, text, numbers and safety, then upload to the account practiceAssets prefix and finalize HTTPS media URLs before creation."
